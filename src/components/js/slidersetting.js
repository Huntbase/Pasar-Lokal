// src/config/sliderSettings.js

const sliderSettings = {
  dots: true, // Menampilkan indikator navigasi
  infinite: true, // Slide berulang tanpa batas
  speed: 500, // Kecepatan transisi
  slidesToShow: 1, // Jumlah slide yang ditampilkan
  slidesToScroll: 1, // Jumlah slide yang digeser per navigasi
  autoplay: true, // Slide otomatis berpindah
  autoplaySpeed: 3000, // Interval waktu untuk auto-slide
  arrows: false, // Sembunyikan tombol panah default
};

export default sliderSettings;
